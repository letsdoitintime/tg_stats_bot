# TG Stats Bot Requirements
# Pinned versions for production stability
# Reconciled against the running venv: 2026-07-22
#
# These pins describe what tgstats-bot / tgstats-migrations ACTUALLY run.
# Before this reconciliation the pins were stale by up to a major version, so
# `pip install -r requirements.txt` (Dockerfile, install_requirements.sh) would
# silently DOWNGRADE prod: redis 6.4->5.2, structlog 25.4->24.4, alembic
# 1.16->1.13, psycopg 3.2->3.1, pydantic 2.11->2.10, fastapi 0.116->0.115,
# uvicorn 0.35->0.32, celery 5.5->5.4, pydantic-settings 2.10->2.7.
# Keep this file in step with the venv; a bump belongs here and in the venv together.

# Core dependencies
python-telegram-bot[all]==22.5.*
sqlalchemy==2.0.*
alembic==1.16.*
psycopg[binary]==3.2.*
asyncpg==0.30.*
pydantic==2.11.*
pydantic-settings==2.10.*
fastapi[all]==0.116.*
# starlette is a fastapi transitive that fastapi declares with NO upper bound, so
# it is not reproducible unless pinned here. fastapi 0.139 pulls starlette 1.3 (a
# major); pinning holds the framework major steady. Bump it WITH fastapi, never alone.
starlette==0.47.*
uvicorn==0.35.*
structlog==25.4.*
emoji==2.14.*
python-dotenv==1.1.*

# Step 2 additions
python-dateutil==2.9.*
tzdata==2025.2
httpx==0.28.*
celery==5.5.*
redis==6.4.*
jinja2==3.1.*
pyyaml==6.0.*

# Performance and monitoring
prometheus-client==0.23.*

# Distributed tracing (genuinely optional — NOT installed in the running venv).
# tgstats/utils/tracing.py guards these imports and sets TRACING_AVAILABLE=False
# when absent, which is the current production state. Listing them as hard
# requirements meant a rebuild would install them and flip tracing ON, changing
# runtime behaviour. Uncomment (all six together) only to deliberately enable it.
# opentelemetry-api==1.28.*
# opentelemetry-sdk==1.28.*
# opentelemetry-instrumentation-fastapi==0.49b2
# opentelemetry-instrumentation-sqlalchemy==0.49b2
# opentelemetry-instrumentation-redis==0.49b2
# opentelemetry-exporter-otlp==1.28.*

# Development dependencies (optional)
# Install with: pip install -r requirements.txt -r requirements-dev.txt
